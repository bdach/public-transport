<UserControl x:Class="PublicTransport.Client.Views.Entities.StopTimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Style="{StaticResource DetailText}">Arrival</TextBlock>
        <TextBlock Grid.Row="0"
                   Grid.Column="1"
                   Margin="10,0,0,0"
                   x:Name="ArrivalTextBlock" />
        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Style="{StaticResource DetailText}">Departure</TextBlock>
        <TextBlock Grid.Row="1"
                   Grid.Column="1"
                   Margin="10 0 0 0"
                   x:Name="DepartureTextBlock" />
    </Grid>
</UserControl>
