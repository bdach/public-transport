<UserControl x:Class="PublicTransport.Client.Views.Entities.RouteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <TextBlock Style="{StaticResource SuperText}"
                   x:Name="ShortNameTextBlock" />
        <StackPanel Grid.Row="0"
                    Grid.Column="1">
            <TextBlock Style="{StaticResource PrimaryText}"
                       x:Name="LongNameTextBlock"  />
            <TextBlock Style="{StaticResource DetailText}"
                       x:Name="AgencyNameTextBlock"  />
        </StackPanel>
    </Grid>
</UserControl>
