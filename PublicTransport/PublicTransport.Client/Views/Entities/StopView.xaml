<UserControl x:Class="PublicTransport.Client.Views.Entities.StopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <TextBlock Style="{StaticResource PrimaryText}"
                   x:Name="StopNameTextBlock" />
        <StackPanel Orientation="Horizontal">
            <TextBlock Style="{StaticResource DetailText}"
                       x:Name="StreetNameTextBlock"
                       Margin="0,0,10,0" />
            <TextBlock Style="{StaticResource DetailText}"
                       x:Name="CityNameTextBlock" />
        </StackPanel>
    </StackPanel>
</UserControl>